<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/iv_credit_loan_compare"
        android:layout_width="116dp"
        android:layout_height="122dp"
        android:src="@drawable/shape_darkgray0_fill_15_rect"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <ImageView
        android:id="@+id/iv_compare"
        android:layout_width="26dp"
        android:layout_height="27dp"
        android:src="@drawable/ic_compare"
        android:layout_marginStart="13dp"
        android:layout_marginTop="15dp"
        app:layout_constraintTop_toTopOf="@id/iv_credit_loan_compare"
        app:layout_constraintStart_toStartOf="@id/iv_credit_loan_compare"/>

    <TextView
        android:id="@+id/tv_credit_loan"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:text="@string/tv_credit_loan"
        android:fontFamily="@font/pretendard_semibold"
        android:textColor="@color/white0"
        android:textSize="14sp"
        android:layout_marginStart="1dp"
        android:layout_marginTop="25dp"
        app:layout_constraintTop_toBottomOf="@id/iv_compare"
        app:layout_constraintStart_toStartOf="@id/iv_compare"/>

    <TextView
        android:id="@+id/tv_compare"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:text="@string/tv_compare"
        android:fontFamily="@font/pretendard_semibold"
        android:textColor="@color/white0"
        android:textSize="14sp"
        android:layout_marginTop="5dp"
        app:layout_constraintTop_toBottomOf="@id/tv_credit_loan"
        app:layout_constraintStart_toStartOf="@id/tv_credit_loan"/>

</androidx.constraintlayout.widget.ConstraintLayout>