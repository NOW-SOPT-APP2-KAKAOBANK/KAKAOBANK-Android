<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.KakaoBank" parent="Theme.Material3.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/main</item>
        <item name="colorOnPrimary">@color/white0</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/dark_gray1</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.KakaoBank" parent="Base.Theme.KakaoBank" />
</resources>